{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Lepascher{% endblock %}

{% block content %}
<div class="footer-closed">
    <h1>Modifier mon de passe</h1>
    -<a href="{{ path ('account') }}">Retour à mon compte</a>

    <hr>
    
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    
    {{ form (form) }}
    <hr>
</div>
{% endblock %}
